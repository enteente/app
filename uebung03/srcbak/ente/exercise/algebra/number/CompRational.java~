package ente.exercise.algebra.number;

import java.lang.*;

/** comparable normrational
 */
public class CompRational implements Comparable extends NormRational{
    private long num;
    private long denom;

    /** constructors
     *
     */

    /** compareTo
     */
    @Override
    public int compareTo(Object o){
        if(!(o instanceof CompRational)){
            throws new IllegalArgumentException("cannot compare Objects because there are not of same type");
        }
        CompRational c = (CompRational) o;
            return this.compare(c);
    }

}
